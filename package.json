{
  "name": "@vitalsdrive/simulator",
  "version": "1.0.0",
  "description": "Ghost Fleet Simulator - Test data pipeline without hardware",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "jest"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/readable-stream": "^2.3.15",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.0"
  }
}
